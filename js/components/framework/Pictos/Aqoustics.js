const React = require('react');
const css = require('./pictos.css');

const Aqoustics = () => {

    return (
        <div className={`${css.pictos} ${css.aqoustics}`}></div>
    );
};

module.exports = Aqoustics;
